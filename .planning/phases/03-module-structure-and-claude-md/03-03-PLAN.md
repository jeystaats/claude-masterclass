---
phase: 03-module-structure-and-claude-md
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/stack.md
  - .claude/rules.md
  - .claude/workflow.md
autonomous: true

must_haves:
  truths:
    - "Root CLAUDE.md is under 80 lines"
    - "Root CLAUDE.md references Next.js 16, React 19, Tailwind v4, and TypeScript 5 (not the outdated Next.js 15 / Tailwind 3.4 from the original)"
    - "Every section in root CLAUDE.md has an inline comment explaining WHY the rule exists"
    - "Root CLAUDE.md uses @imports for detailed sections rather than inlining the content"
    - ".claude/stack.md, .claude/rules.md, and .claude/workflow.md exist and contain the full detail"
    - "@import paths are relative (not absolute) so they work when any student clones the repo"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Root project guide for Claude Code, under 80 lines"
      contains: "@.claude/stack.md"
    - path: ".claude/stack.md"
      provides: "Full stack table with versions and constraints"
      contains: "Next.js 16"
    - path: ".claude/rules.md"
      provides: "Coding standards Claude can verify and enforce"
      contains: "TypeScript"
    - path: ".claude/workflow.md"
      provides: "Command reference and workflow patterns"
      contains: "pnpm"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/stack.md"
      via: "@import"
      pattern: "@\\.claude/stack\\.md"
    - from: "CLAUDE.md"
      to: ".claude/rules.md"
      via: "@import"
      pattern: "@\\.claude/rules\\.md"
    - from: "CLAUDE.md"
      to: ".claude/workflow.md"
      via: "@import"
      pattern: "@\\.claude/workflow\\.md"
    - from: "modules/*/. claude/CLAUDE.md (plan 03-04)"
      to: "root CLAUDE.md"
      via: "overrides with explicit <!-- Overrides root CLAUDE.md --> comment"
      pattern: "Overrides root"
---

<objective>
Replace the existing 280-line root CLAUDE.md with a teaching-quality file under 80 lines, backed by three imported sub-files.

Purpose: The existing CLAUDE.md references the wrong stack (Next.js 15, Tailwind 3.4) and is too long to serve as a teaching artifact. The replacement demonstrates the @import pattern — a core technique students learn in Module 2 (CLAUDE.md Mastery). Every WHY comment teaches while guiding.
Output: CLAUDE.md (root, under 80 lines) + .claude/stack.md + .claude/rules.md + .claude/workflow.md
</objective>

<execution_context>
@/Users/jasperstaats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasperstaats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/tmp/claude-masterclass/.planning/phases/03-module-structure-and-claude-md/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write root CLAUDE.md (under 80 lines, @import structure)</name>
  <files>CLAUDE.md</files>
  <action>
    Overwrite the existing CLAUDE.md at /tmp/claude-masterclass/CLAUDE.md entirely. The new content:

    ```markdown
    # Claude Code Mastery — Workshop Guide

    You are Claude Code, helping a student build a real product during the Claude Code Mastery course.
    Read this file before every response. Follow every rule without exception.

    ---

    ## Stack
    <!-- Why: Without a stack declaration, Claude will suggest whatever library it prefers.
         Declaring it here means every answer stays in the same ecosystem. -->
    @.claude/stack.md

    ## Coding rules
    <!-- Why: Rules here are instructions Claude reads literally — be specific, not aspirational.
         "Write clean code" is meaningless. "No inline styles" is enforceable. -->
    @.claude/rules.md

    ## Workflow
    <!-- Why: Students follow a consistent workflow so they build habits, not just features.
         Documenting commands here stops Claude from guessing or suggesting npm when pnpm is correct. -->
    @.claude/workflow.md

    ---

    ## How to use this project

    You are working on a course project. Each session, open a module folder in `modules/` and
    start from the README.md. Use PROGRESS.md to track what you've finished.

    When you start a new feature, tell Claude:
    - What module you're in
    - What lesson you're doing
    - What you want to build

    Example: "I'm in Module 4, lesson Writing a PRD. Help me fill in modules/04-plan-your-product/exercises/PRD.md for my invoice tracker app."

    ---

    ## For Claude: tone and behavior

    <!-- Why: Behavior rules are as important as technical rules for a teaching environment. -->
    - This is a learning environment. Explain decisions before implementing them.
    - When a student makes a mistake, explain what went wrong before fixing it.
    - Prefer working examples over elegant abstractions.
    - Ask one clarifying question at a time — never a list of questions.
    - When multiple approaches exist, recommend one and explain why.
    ```

    Line count target: under 80 lines. Count lines before writing. The above template is approximately 55 lines — leave room for any adjustments.

    CRITICAL: Do NOT reference absolute paths, personal directories, or platform-specific content. All @imports must use relative paths (`@.claude/stack.md`, not `@/Users/.../stack.md`).

    CRITICAL: Do NOT use the content from the existing CLAUDE.md — it has the wrong stack and wrong format. Write from scratch using only the content specified above.
  </action>
  <verify>
    Run: `wc -l CLAUDE.md` — must return 80 or fewer
    Run: `grep "@.claude/stack.md" CLAUDE.md` — must return the import line
    Run: `grep "@.claude/rules.md" CLAUDE.md` — must return the import line
    Run: `grep "@.claude/workflow.md" CLAUDE.md` — must return the import line
    Run: `grep "Next.js 15\|Tailwind 3.4" CLAUDE.md` — must return nothing (wrong stack not present)
    Run: `grep "Why:" CLAUDE.md` — must return at least 3 inline WHY comments
  </verify>
  <done>
    CLAUDE.md is under 80 lines. It contains @imports for all three sub-files. Every section has an inline <!-- Why: --> comment. No absolute paths. Stack references (if any inline) say Next.js 16, React 19, Tailwind v4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .claude/stack.md, .claude/rules.md, and .claude/workflow.md</name>
  <files>
    .claude/stack.md
    .claude/rules.md
    .claude/workflow.md
  </files>
  <action>
    Create .claude/stack.md:

    ```markdown
    # Stack

    Use **only** these technologies. Do not suggest alternatives unless the student explicitly asks.

    | Layer | Technology | Version | Notes |
    |-------|-----------|---------|-------|
    | Framework | Next.js | 16 | App Router only — no Pages Router |
    | UI | React | 19 | Use Server Components where possible |
    | Language | TypeScript | 5 | Strict mode, no `any` |
    | Styling | Tailwind CSS | v4 | CSS-first config — no tailwind.config.js |
    | Auth | Clerk | latest | Always Clerk for auth, never roll your own |
    | Payments | Stripe | latest | Always Stripe for payments |
    | Package manager | pnpm | 9 | Never npm or yarn |

    ## When students ask "what should I use for X?"

    | Need | Use | Never suggest |
    |------|-----|---------------|
    | Authentication | Clerk | NextAuth, Auth.js, custom JWT |
    | Payments | Stripe | Paddle, LemonSqueezy (unless student explicitly wants it) |
    | Database | Convex or Supabase (student picks in Module 4) | Firebase, MongoDB, PlanetScale |
    | Styling | Tailwind CSS v4 | CSS Modules, styled-components, Emotion |
    | State management | React built-ins (useState, useContext) | Redux, Zustand, Jotai |
    ```

    Create .claude/rules.md:

    ```markdown
    # Coding Rules

    These rules apply to every file in this project. Follow them without exception.

    ## TypeScript
    - Strict mode is on — no `any`, no `@ts-ignore`
    - Types in separate `.types.ts` files when complex (3+ properties)
    - Use `interface` for object shapes, `type` for unions and aliases
    - No implicit `any` — every function parameter must be typed

    ## React and Next.js
    - App Router only — no `getServerSideProps`, no `getStaticProps`
    - Server Components by default — add `"use client"` only when needed (event handlers, hooks, browser APIs)
    - Named exports only — never `export default` for components
    - One component per file

    ## Tailwind CSS v4
    - Use design tokens from `globals.css`, never hardcode hex colors
    - Use `cn()` for conditional classes — never string concatenation
    - No inline `style={{}}` attributes

    ## File organization
    - Components: `src/components/[domain]/[component-name].tsx`
    - Pages: `src/app/[route]/page.tsx`
    - API routes: `src/app/api/[route]/route.ts`
    - Shared types: `src/types/[domain].types.ts`

    ## Code quality
    - Components under 100 lines — split if longer
    - No commented-out code in commits
    - Every `fetch()` call has error handling
    - Validate external data with Zod before using it
    ```

    Create .claude/workflow.md:

    ```markdown
    # Workflow

    ## Commands

    | Task | Command |
    |------|---------|
    | Install dependencies | `pnpm install` |
    | Start dev server | `pnpm dev` (runs on port 3000) |
    | Type check | `pnpm typecheck` |
    | Lint | `pnpm lint` |
    | Build | `pnpm build` |

    ## Git workflow
    - Feature branches: `feat/[description]`
    - Bug fixes: `fix/[description]`
    - Never commit directly to `main` — always use a branch + PR
    - Run `pnpm typecheck` before committing

    ## Working with modules
    1. Open the module folder: `modules/NN-[slug]/`
    2. Read `README.md` for objectives
    3. Open lesson files or `exercises/` folder
    4. Use `PROGRESS.md` to track completion

    ## Starting a new feature
    1. Create a feature brief in `exercises/feature-brief.md` (Module 6 pattern)
    2. Ask Claude to review the brief before writing code
    3. Build one ticket at a time — verify before moving on
    4. Commit working increments with semantic commit messages

    ## Semantic commit format
    ```
    feat(scope): add invoice creation form
    fix(auth): resolve redirect after login
    ```
    Types: feat, fix, refactor, docs, style, chore
    ```
  </action>
  <verify>
    Run: `ls .claude/` — lists stack.md, rules.md, workflow.md
    Run: `grep "Next.js" .claude/stack.md` — returns row with "16"
    Run: `grep "Tailwind CSS" .claude/stack.md` — returns row with "v4"
    Run: `grep "React" .claude/stack.md` — returns row with "19"
    Run: `grep "pnpm" .claude/workflow.md` — returns the commands table entries
    Run: `grep "any" .claude/rules.md` — returns the TypeScript no-any rule
  </verify>
  <done>
    Three sub-files exist in .claude/. stack.md has the full stack table with versions matching requirements (Next.js 16, React 19, Tailwind v4, TypeScript 5). rules.md has actionable coding rules. workflow.md has the pnpm command table and git workflow.
  </done>
</task>

</tasks>

<verification>
1. `wc -l CLAUDE.md` returns 80 or fewer
2. `grep -c "@.claude/" CLAUDE.md` returns 3 (three @import lines)
3. `grep "Next.js 15\|Tailwind 3.4\|React 18" CLAUDE.md .claude/stack.md` returns nothing (old stack not present anywhere)
4. `grep "Next.js.*16\|Tailwind.*v4\|React.*19\|TypeScript.*5" .claude/stack.md` returns matches for all four
5. `grep "<!-- Why:" CLAUDE.md | wc -l` returns 3 or more WHY comments in root file
</verification>

<success_criteria>
- Root CLAUDE.md is under 80 lines with @imports for stack.md, rules.md, workflow.md
- All three .claude/ sub-files exist and are non-empty
- Stack is correct: Next.js 16, React 19, Tailwind v4, TypeScript 5
- @import paths are relative (not absolute)
- Every major section in root CLAUDE.md has a <!-- Why: --> comment
- No content from the old 280-line CLAUDE.md (wrong stack) survives
</success_criteria>

<output>
After completion, create `/tmp/claude-masterclass/.planning/phases/03-module-structure-and-claude-md/03-03-SUMMARY.md`
</output>
