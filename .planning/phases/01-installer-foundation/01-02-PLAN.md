---
phase: 01-installer-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .gitignore
  - config/workshop-settings.json
autonomous: true

must_haves:
  truths:
    - ".env.example documents all required environment variables for the course project (Anthropic, Convex, Clerk, Stripe)"
    - ".gitignore covers .env, node_modules, .DS_Store, Claude Code logs, and build output"
    - "config/workshop-settings.json contains a valid Claude Code settings template ready for merge"
  artifacts:
    - path: ".env.example"
      provides: "Template for all required environment variables"
      contains: "ANTHROPIC_API_KEY"
    - path: ".gitignore"
      provides: "Git ignore rules for env files, deps, OS files, Claude Code logs"
      contains: ".claude/settings.local.json"
    - path: "config/workshop-settings.json"
      provides: "Workshop Claude Code settings template for merge into ~/.claude/settings.json"
      contains: "hooks"
  key_links:
    - from: "config/workshop-settings.json"
      to: "install.sh merge logic (Plan 03)"
      via: "jq merge reads this file as incoming config"
      pattern: "workshop-settings.json"
---

<objective>
Create .env.example, update .gitignore, and create the workshop settings.json template that the installer will merge into ~/.claude/settings.json.

Purpose: SCAF-03 and SCAF-04 are installer prerequisites. The .env.example documents what students need to configure. The .gitignore prevents secrets from being committed. The workshop-settings.json is the config template Plan 03's merge logic will use.
Output: Three files ready for use by students and the installer.
</objective>

<execution_context>
@/Users/jasperstaats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasperstaats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-installer-foundation/01-RESEARCH.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example with documented variables</name>
  <files>.env.example</files>
  <action>
Create `.env.example` at project root with all required environment variables, grouped by service. Each group gets a comment header. Each variable gets a placeholder value showing the expected format.

Groups and variables:

```
# ===========================================
# Claude Code Mastery — Environment Variables
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# -------------------------------------------
# Anthropic / Claude
# -------------------------------------------
ANTHROPIC_API_KEY=sk-ant-...

# -------------------------------------------
# Convex (Backend)
# -------------------------------------------
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# -------------------------------------------
# Clerk (Authentication)
# -------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# -------------------------------------------
# Stripe (Payments — optional for early modules)
# -------------------------------------------
# STRIPE_SECRET_KEY=sk_test_...
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
```

Stripe variables are commented out since they're optional for early modules. All other variables are uncommented with placeholder values.
  </action>
  <verify>
1. File exists: `test -f .env.example`
2. Contains all required keys: `grep -c "ANTHROPIC_API_KEY\|CONVEX_DEPLOYMENT\|NEXT_PUBLIC_CONVEX_URL\|CLERK_SECRET_KEY\|NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY" .env.example` returns 5
3. Stripe vars are commented out: `grep "^# STRIPE" .env.example` returns matches
  </verify>
  <done>.env.example exists with all required variables documented, grouped by service, with placeholder format hints. Stripe vars are commented out as optional.</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore and create config/workshop-settings.json</name>
  <files>.gitignore, config/workshop-settings.json</files>
  <action>
**Part A: Update .gitignore**

The existing .gitignore already covers node_modules, .env*, .DS_Store, .next/, and build output. Add these MISSING entries at the end, under clear comment headers:

```gitignore
# Claude Code logs and local settings
.claude/settings.local.json
.claude/logs/

# pnpm
.pnpm-store/
```

Do NOT remove any existing entries. Append only. The existing file already handles `.env*` (covers .env, .env.local, .env.*.local), node_modules, .DS_Store, .next, /build, and /out.

**Part B: Create config/workshop-settings.json**

Create the `config/` directory and `config/workshop-settings.json`. This is the template that Plan 03's merge logic will merge into the student's `~/.claude/settings.json`.

For Phase 1, this is a minimal skeleton — hooks will be populated in Phase 2 (Global Config Content). The structure must match the Claude Code settings.json schema:

```json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [],
    "PostToolUse": [],
    "Stop": []
  },
  "permissions": {
    "allow": [],
    "deny": []
  }
}
```

This empty-arrays template is intentional — it establishes the merge target structure without overwriting any existing student hooks. Phase 2 will populate the arrays with teaching hooks.
  </action>
  <verify>
1. .gitignore contains Claude Code entries: `grep "claude/settings.local" .gitignore`
2. `config/workshop-settings.json` exists and is valid JSON: `cat config/workshop-settings.json | python3 -m json.tool > /dev/null`
3. workshop-settings.json contains hooks key: `grep '"hooks"' config/workshop-settings.json`
  </verify>
  <done>.gitignore updated with Claude Code log/settings entries. config/workshop-settings.json exists with valid Claude Code settings schema skeleton (empty hook arrays ready for Phase 2 content).</done>
</task>

</tasks>

<verification>
- `.env.example` documents Anthropic, Convex, Clerk, and Stripe (commented) variables
- `.gitignore` covers .env, node_modules, .DS_Store, Claude Code logs, build output
- `config/workshop-settings.json` is valid JSON matching Claude Code settings schema
- No existing .gitignore entries were removed
</verification>

<success_criteria>
A student can `cp .env.example .env` and see exactly which variables to fill in. Committing never accidentally includes .env files or Claude Code local settings. The workshop settings template is ready for the merge logic in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/01-installer-foundation/01-02-SUMMARY.md`
</output>
