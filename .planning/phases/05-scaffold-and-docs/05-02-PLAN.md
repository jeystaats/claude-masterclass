---
phase: 05-scaffold-and-docs
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "A new student can read the README and run their first Claude Code session in 5 minutes or fewer"
    - "The Quick Start section has exactly 5 numbered steps with no ambiguity"
    - "EACCES errors, 'command not found claude', and PowerShell execution policy blocks each have a dedicated troubleshooting fix"
    - "No references to Next.js 15 or non-existent paths like /workshop-start"
    - "README is under 150 lines and fully scannable — no walls of text"
  artifacts:
    - path: "README.md"
      provides: "Complete getting-started guide for course students"
      min_lines: 40
  key_links:
    - from: "README.md Quick Start step 2"
      to: "install.sh"
      via: "`bash install.sh` command"
      pattern: "bash install\\.sh"
    - from: "README.md Quick Start step 3"
      to: "start.sh"
      via: "`bash start.sh` command"
      pattern: "bash start\\.sh"
    - from: "README.md Troubleshooting EACCES"
      to: "claude.ai native installer"
      via: "curl install command"
      pattern: "curl.*claude\\.ai/install"
---

<objective>
Replace the placeholder README with a complete, student-ready getting-started guide that gets anyone from clone to first Claude Code session in 5 minutes.

Purpose: The current README says "Next.js 15", references /workshop-start (which doesn't exist), and has no troubleshooting. It actively misleads students on day one. This plan replaces it entirely.

Output: README.md — complete, under 150 lines, with hero, prerequisites, 5-step quick start, what's included table, troubleshooting for the three most common failure modes, and next steps.
</objective>

<execution_context>
@/Users/jasperstaats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasperstaats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
    Replace the entire contents of README.md with the following. Write it verbatim — do not paraphrase, summarize, or reformat. Every detail is load-bearing for the 5-minute goal.

    ---

    # Claude Code Mastery — Starter Kit

    A working Next.js 16 app you can build in immediately. Clone it, run two commands, and you're ready for your first Claude Code session.

    ---

    ## Prerequisites

    - **Node.js 20+** — [nodejs.org](https://nodejs.org)
    - **pnpm 9+** — `npm install -g pnpm@9`
    - **Claude Code** — [claude.ai/code](https://claude.ai/code)
    - **Git**

    ---

    ## Quick Start

    ```bash
    # 1. Clone the repo
    git clone https://github.com/your-org/claude-masterclass-starter.git
    cd claude-masterclass-starter

    # 2. Install dependencies and Claude Code config
    bash install.sh

    # 3. Start the dev server
    bash start.sh
    ```

    4. Open the module you're working on in the course and read the brief.
    5. Open Claude Code in your project folder: `claude` — then describe your first task.

    The app runs at **http://localhost:3000**.

    ---

    ## What's Included

    | Path | What it is |
    |------|------------|
    | `src/app/` | Next.js App Router pages |
    | `src/lib/utils.ts` | `cn()` utility (clsx + tailwind-merge) |
    | `.claude/` | Claude Code config: agents, skills, rules |
    | `install.sh` | Installs deps + copies Claude config |
    | `start.sh` | Starts dev server |

    ---

    ## Troubleshooting

    ### EACCES: permission denied when installing Claude Code

    Never use `sudo npm install -g @anthropic-ai/claude-code`. Use the native installer instead:

    ```bash
    curl -fsSL https://claude.ai/install.sh | sh
    ```

    This installs to `~/.local/bin`, which doesn't require root access.

    ---

    ### `claude: command not found` after install

    The installer puts Claude Code in `~/.local/bin`. Add it to your PATH:

    ```bash
    # Add to ~/.zshrc or ~/.bashrc
    export PATH="$HOME/.local/bin:$PATH"

    # Apply immediately
    source ~/.zshrc   # or source ~/.bashrc
    ```

    Then verify: `claude --version`

    ---

    ### PowerShell: "running scripts is disabled on this system"

    Open PowerShell **as Administrator** and run:

    ```powershell
    Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
    ```

    Then close and reopen your terminal.

    ---

    ## Next Steps

    Once the server is running and Claude Code is open, pick your first module from the course portal and follow its brief. Each module is self-contained — you build one thing, ship it, and move on.

    See `UPGRADING.md` to personalize your Claude config and grow beyond course defaults.

    ---

    (end of README content)

    Notes for implementation:
    - The repo URL placeholder `https://github.com/your-org/claude-masterclass-starter.git` is intentional — leave it as-is. Students will use their own fork URL.
    - Keep the `---` horizontal rules as section separators — they make the README scannable.
    - Do not add a table of contents — it adds length without helping the 5-minute goal.
    - Line count target: 90-120 lines. Do not exceed 150.
  </action>
  <verify>
    Check line count:
    ```bash
    wc -l README.md
    ```
    Must be under 150.

    Verify no references to wrong version or non-existent paths:
    ```bash
    grep -n "Next.js 15\|workshop-start\|localhost:3001" README.md
    ```
    Must return no matches.

    Verify troubleshooting sections exist:
    ```bash
    grep -c "EACCES\|command not found\|execution policy\|ExecutionPolicy" README.md
    ```
    Must return 4 or more (each term appears at least once).

    Verify install.sh and start.sh are referenced:
    ```bash
    grep -c "install\.sh\|start\.sh" README.md
    ```
    Must return 2 or more.
  </verify>
  <done>
    README.md is under 150 lines. Contains no references to "Next.js 15" or "/workshop-start". All three troubleshooting scenarios (EACCES, command not found, ExecutionPolicy) are present. Quick Start has exactly 5 steps. install.sh and start.sh are both referenced.
  </done>
</task>

</tasks>

<verification>
Read the README as a new student would and time the critical path:

1. Read Prerequisites — can you tell in 10 seconds if you're ready?
2. Read Quick Start — can you follow steps 1-5 without opening any other doc?
3. Read Troubleshooting — does each fix tell you exactly what to run?

The 5-minute clock starts at step 1 of Quick Start and ends when `claude` opens in the project folder. Nothing in the README should require a student to leave the page to understand what to do next.

Also run:
```bash
grep -n "TODO\|FIXME\|placeholder\|TBD\|\[to be\]" README.md
```
Must return zero matches (except the intentional repo URL placeholder, which is not a TODO).
</verification>

<success_criteria>
- README.md exists and is under 150 lines
- No references to "Next.js 15", "/workshop-start", or other stale content from the old placeholder
- Quick Start has 5 steps that lead directly to an open Claude Code session
- Three troubleshooting sections: EACCES (curl native installer), command not found (PATH fix), PowerShell execution policy (Set-ExecutionPolicy command)
- "What's Included" table accurately reflects the actual project structure
- UPGRADING.md is referenced at the end for students ready to go further
</success_criteria>

<output>
After completion, create `.planning/phases/05-scaffold-and-docs/05-02-SUMMARY.md` documenting:
- Final line count of README.md
- Any content decisions made (e.g., what was excluded to stay under 150 lines)
- Whether the repo URL placeholder was left intentional or updated
</output>
