---
phase: 05-scaffold-and-docs
plan: "03"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - UPGRADING.md
autonomous: true

must_haves:
  truths:
    - "A student can find and understand how to rename lah-* files to their own prefix"
    - "A student can understand how to add their own agents, skills, and hooks alongside course defaults"
    - "A student can update the starter kit via git pull without losing their personalizations"
    - "The path from course defaults to production CLAUDE.md standards is explained, not just mentioned"
    - "UPGRADING.md is under 100 lines"
  artifacts:
    - path: "UPGRADING.md"
      provides: "Post-course personalization and evolution guide"
      min_lines: 30
  key_links:
    - from: "UPGRADING.md personalize section"
      to: ".claude/ directory structure"
      via: "file path references to lah-* files"
      pattern: "lah-"
    - from: "UPGRADING.md update section"
      to: "install.sh"
      via: "`bash install.sh` is idempotent"
      pattern: "install\\.sh"
---

<objective>
Write UPGRADING.md — the guide for students who finish the course and want to personalize their Claude config, extend it with their own agents and skills, and stay updated as the course material evolves.

Purpose: Without this guide, students hit a wall after the final module. They have a working Claude config but don't know how to make it theirs, add new capabilities, or pull future course updates without clobbering their changes. This doc answers all three questions concisely.

Output: UPGRADING.md — under 100 lines, practical, no aspirational filler. Four sections: personalize, extend, evolve, update.
</objective>

<execution_context>
@/Users/jasperstaats/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jasperstaats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write UPGRADING.md</name>
  <files>UPGRADING.md</files>
  <action>
    Create UPGRADING.md at the project root with the following content. Write it verbatim.

    ---

    # Upgrading Beyond the Course Defaults

    This guide is for after you've completed the course — when you're ready to make the Claude config truly yours.

    ---

    ## 1. Personalize

    The course ships a set of agents and skills prefixed `lah-` (like-a-human). These are teaching examples. Replace them with your own.

    **Rename the prefix:**
    ```bash
    # Example: rename lah-reviewer to your own handle
    mv .claude/agents/lah-reviewer.md .claude/agents/yourname-reviewer.md
    ```

    Update any references to the old name in your `.claude/CLAUDE.md` or other agent files.

    **Swap the global CLAUDE.md:**
    The course installs `.claude/CLAUDE.md` with conservative defaults tuned for learning. Once you're past the basics, replace it with your own. The course CLAUDE.md has comments marking each opinionated choice — use those as a starting point.

    ---

    ## 2. Extend

    Add your own agents, skills, and hooks alongside the course files — don't delete the originals until you're sure you don't need them as reference.

    **Add an agent:**
    ```bash
    # Copy a lah-* file as a template
    cp .claude/agents/lah-reviewer.md .claude/agents/my-reviewer.md
    # Edit the name, description, and instructions
    ```

    **Add a skill:**
    ```bash
    cp .claude/skills/lah-example.md .claude/skills/my-skill.md
    ```

    **Add a hook:**
    Hooks live in `.claude/hooks/`. Each hook is a shell script that runs at a lifecycle point (pre-commit, post-tool-use, etc.). Copy an existing hook and modify the trigger and command.

    See the [Claude Code docs on hooks](https://docs.anthropic.com/claude-code/hooks) for the full lifecycle reference.

    ---

    ## 3. Evolve

    The course defaults are intentionally conservative — good for learning, not always right for production. Module 9 covers migrating to a production-grade CLAUDE.md.

    Key differences from course defaults to production standards:
    - **Hooks:** Course hooks are illustrative. Production hooks enforce real quality gates (typecheck, lint, test).
    - **Agent autonomy:** Course agents ask for confirmation often. Production agents run more autonomously with `--dangerously-skip-permissions` on trusted tasks.
    - **CLAUDE.md scope:** The course uses a single global file. Production setups layer global + per-project + per-directory configs.

    When you're ready, follow Module 9's migration guide. It's a two-hour exercise, not a rewrite.

    ---

    ## 4. Update

    The starter kit is updated as new course modules ship. Pull updates without losing your changes:

    ```bash
    # Pull course updates
    git pull origin main

    # Re-run install.sh — it's idempotent (safe to run multiple times)
    # It will not overwrite files you've modified
    bash install.sh
    ```

    If you want to refresh a specific course file, delete it first then re-run install.sh:
    ```bash
    # Force-refresh a specific course file by removing it first
    rm .claude/agents/lah-reviewer.md
    bash install.sh
    ```

    If you hit merge conflicts in `.claude/`, keep your version — your personalizations take precedence over course updates.

    ---

    (end of UPGRADING.md content)

    Notes for implementation:
    - The delete-then-reinstall pattern above accurately describes how install.sh works: it skips existing files, so removing first is the correct way to force a refresh of a single file.
    - Keep the link to Claude Code docs on hooks — it's the only external link in the file and it's load-bearing.
    - Do not add a "what's next" section. Students who read this are self-directed; they don't need encouragement.
    - Line count target: 70-90 lines. Do not exceed 100.
  </action>
  <verify>
    Check line count:
    ```bash
    wc -l UPGRADING.md
    ```
    Must be under 100.

    Verify all four sections are present:
    ```bash
    grep -c "^## [0-9]\." UPGRADING.md
    ```
    Must return 4.

    Verify lah-* references exist (the personalization hook):
    ```bash
    grep -c "lah-" UPGRADING.md
    ```
    Must return 3 or more.

    Verify install.sh is referenced as idempotent:
    ```bash
    grep -c "install\.sh" UPGRADING.md
    ```
    Must return 2 or more.

    Verify Module 9 is referenced:
    ```bash
    grep -c "Module 9" UPGRADING.md
    ```
    Must return 1 or more.

    Verify no --force flag reference (install.sh does not support it):
    ```bash
    grep "install\.sh --force" UPGRADING.md
    ```
    Must return no matches.
  </verify>
  <done>
    UPGRADING.md exists at project root, is under 100 lines, has four numbered sections (Personalize, Extend, Evolve, Update), references lah-* files with rename examples, explains install.sh idempotency, links evolution to Module 9, and uses the correct delete-then-reinstall pattern for force-refreshing individual files.
  </done>
</task>

</tasks>

<verification>
Read UPGRADING.md against the three questions it must answer:

1. "How do I make this Claude config mine?" — Answered in section 1 (rename prefix, swap CLAUDE.md)
2. "How do I add my own agents/skills?" — Answered in section 2 (copy lah-* as template)
3. "How do I pull course updates without losing my changes?" — Answered in section 4 (git pull + idempotent install.sh)

If any of these three questions requires reading another document to answer, the plan is incomplete.

Also check:
```bash
wc -l UPGRADING.md
```
Must be 100 or fewer.
</verification>

<success_criteria>
- UPGRADING.md exists at project root
- Under 100 lines
- Four sections: Personalize, Extend, Evolve, Update (numbered)
- Section 1 shows how to rename lah-* files with a concrete bash example
- Section 2 shows how to add agents/skills/hooks using course files as templates
- Section 3 explains the course-to-production transition and points to Module 9
- Section 4 explains git pull + `bash install.sh` as the update workflow, with note that install.sh is idempotent and the correct delete-then-reinstall pattern for force-refreshing individual files
- No aspirational filler ("you're now ready to...", "the sky is the limit") — practical only
- No `install.sh --force` references (install.sh does not support a --force flag)
</success_criteria>

<output>
After completion, create `.planning/phases/05-scaffold-and-docs/05-03-SUMMARY.md` documenting:
- Final line count of UPGRADING.md
- Any content decisions (what was cut to stay under 100 lines)
- The update workflow used (delete-then-reinstall pattern, not --force flag)
</output>
